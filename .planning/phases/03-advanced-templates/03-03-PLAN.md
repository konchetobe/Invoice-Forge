---
phase: 03-advanced-templates
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/Services/EmailService.php
  - templates/email/invoice-sent.php
autonomous: false
requirements:
  - TMPL-06

must_haves:
  truths:
    - "Invoice email is sent as HTML (not plain text) with styled summary body"
    - "Email body includes invoice number, client name, total, due date, line items summary, and Pay Invoice placeholder button"
    - "PDF invoice is still attached to the email"
    - "Email HTML uses only inline styles (no style blocks or class attributes)"
    - "Admin can visually verify the PDF output matches expected format"
  artifacts:
    - path: "src/Services/EmailService.php"
      provides: "HTML email body via PdfService renderEmailBody()"
      contains: "renderEmailBody"
    - path: "templates/email/invoice-sent.php"
      provides: "Updated email template (if used)"
  key_links:
    - from: "src/Services/EmailService.php"
      to: "src/Services/PdfService.php"
      via: "renderEmailBody() call"
      pattern: "renderEmailBody"
    - from: "src/Services/EmailService.php"
      to: "wp_mail"
      via: "Content-Type text/html header"
      pattern: "text/html"
---

<objective>
Wire the HTML email body into EmailService and provide a visual verification checkpoint for the complete template system.

Purpose: Complete the Phase 3 deliverable by replacing the plain-text email body with a styled HTML body generated from the template (email mode), and verify the entire template system end-to-end.

Output: EmailService sends HTML emails with PDF attachment. User visually confirms PDF and email output quality.
</objective>

<execution_context>
@C:/Users/Ananaska/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ananaska/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced-templates/03-CONTEXT.md
@.planning/phases/03-advanced-templates/03-RESEARCH.md
@.planning/phases/03-advanced-templates/03-01-SUMMARY.md
@.planning/phases/03-advanced-templates/03-02-SUMMARY.md

@src/Services/EmailService.php
@src/Services/PdfService.php

<interfaces>
<!-- From Plan 02 -->

PdfService extended API:
```php
public function generate(int $invoice_id, string $output_mode = 'S', string $file_path = '', string $render_mode = 'pdf'): ?string
public function renderEmailBody(int $invoice_id): string  // Returns HTML for email body
public function getInvoiceData(int $invoice_id): ?array     // Existing, extended with payment_method
```

Existing EmailService::sendInvoice() currently:
- Gets invoice data via $this->pdfService->getInvoiceData()
- Builds plain-text $message via sprintf
- Sets Content-Type: text/plain
- Attaches PDF via temp file
- Sends via wp_mail()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire HTML email body into EmailService</name>
  <files>src/Services/EmailService.php</files>
  <action>
    **Replace plain-text email body with HTML body** in `sendInvoice()`:

    1. Instead of building `$message` via `sprintf()` with plain text, call `$this->pdfService->renderEmailBody($invoice_id)` to get the styled HTML body.

    2. Change the Content-Type header from `text/plain; charset=UTF-8` to `text/html; charset=UTF-8`.

    3. Keep all other logic intact: PDF attachment generation, temp file handling, from name/email, `wp_mail()` call, cleanup, logging, post meta update.

    4. Add a fallback: if `renderEmailBody()` returns empty string or throws, fall back to a simple HTML message (not plain text — keep Content-Type as text/html):
    ```php
    $message = $this->pdfService->renderEmailBody($invoice_id);
    if (empty($message)) {
        $message = '<html><body><p>' . sprintf(
            esc_html__('Please find your invoice %s attached.', 'invoiceforge'),
            esc_html($invoice['number'])
        ) . '</p><p><strong>' . esc_html__('Amount Due:', 'invoiceforge') . ' '
        . esc_html($invoice['currency']) . ' '
        . esc_html(number_format($invoice['total_amount'], 2))
        . '</strong></p></body></html>';
    }
    ```

    5. Do the same for `sendReminder()` — change to `text/html` Content-Type, wrap the existing plain text in minimal HTML tags (`<html><body><p>...</p></body></html>`). The reminder does not need the full template treatment — just convert to HTML so both email types use the same content type.

    6. If `templates/email/invoice-sent.php` exists, check whether it's used anywhere. If it's a standalone template file not currently included by EmailService, leave it as-is — the HTML body now comes from PdfService's template in email mode.
  </action>
  <verify>
    <automated>grep -n "text/html\|renderEmailBody\|Content-Type" src/Services/EmailService.php</automated>
  </verify>
  <done>
    - sendInvoice() uses PdfService::renderEmailBody() for HTML body
    - Content-Type header is text/html for both sendInvoice and sendReminder
    - PDF still attached to invoice emails
    - Fallback HTML body generated if renderEmailBody fails
    - sendReminder() converted to HTML content type with wrapped message
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Advanced Templates system</name>
  <action>
    Visual and functional verification of the complete Phase 3 Advanced Templates system.

    What was built:
    - Settings > Template tab with logo upload, accent color, payment methods, section visibility, drag-and-drop section ordering, signature field configuration
    - Company profile fields (ID No, Office, Att To, Bank/IBAN/BIC) in Settings > General
    - Client profile fields (ID No, Office, Att To) in client editor
    - Payment method dropdown in invoice editor
    - Configurable PDF template with Bulgarian business invoice format (three-column header, conditional discount/bank/signature sections)
    - HTML email body with invoice summary and "Pay Invoice" placeholder button
    - PDF attachment on invoice emails

    How to verify:

    1. Go to WP Admin > InvoiceForge > Settings > Template tab
       - Upload a logo image, set accent color, add/remove payment methods, toggle section visibility, drag sections to reorder, add/edit signature fields
       - Save and reload — verify all values persist

    2. Go to Settings > General tab
       - Verify company ID No, Office, Att To, Bank Name, IBAN, BIC fields are present
       - Fill in values and save

    3. Edit a client record
       - Verify ID No, Office, Att To fields are present
       - Fill in and save

    4. Edit an invoice
       - Verify payment method dropdown is present with configured options
       - Select "Bank transfer" and save

    5. Preview/download the invoice PDF
       - Verify three-column header layout (BUYER | Invoice No+Date | SELLER)
       - Verify logo appears in SELLER section (if uploaded)
       - Verify accent color applied to headers and totals
       - Verify line item table shows discount columns only if discounts exist
       - Verify Bank/IBAN section appears (Bank transfer selected)
       - Verify signature block renders with configured fields
       - Verify sections appear in configured order

    6. Send the invoice via email
       - Check received email is HTML formatted (not plain text)
       - Verify email shows invoice summary, line items, total
       - Verify "Pay Invoice" button/link is present (placeholder)
       - Verify PDF is attached

    7. Change payment method to "Cash", regenerate PDF
       - Verify Bank/IBAN section is hidden
  </action>
  <verify>User confirms all checks pass</verify>
  <done>User types "approved" confirming complete template system works correctly</done>
</task>

</tasks>

<verification>
- Invoice email arrives as HTML with styled summary body
- PDF is still attached to the email
- Email renders correctly in a web email client (inline styles, no broken layout)
- Visual confirmation of PDF layout matching the Bulgarian business invoice reference format
</verification>

<success_criteria>
- Email body is HTML (not plain text) with invoice summary and Pay Invoice button
- PDF attachment still works
- Complete template system verified end-to-end by user
- All Phase 3 features functional: template config, PDF rendering, email HTML body
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-templates/03-03-SUMMARY.md`
</output>
